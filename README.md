# Projeto de geolocalização que usa a base de dados dos correios
## Para baixar a aplicação
<div>`git clone git@github.com:reinaldoper/geolocation.git`</div> < /br>
`npm install` na raiz da aplicação
